Run via GPA_GUI_V6.py or use the executable GPA_GUI_V6.exe (only works with windows)

Then, select data using interface and hit calculate

A report will be generated called [RUNFILENAME].RUN.STATS in the same folder